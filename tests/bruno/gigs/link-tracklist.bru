meta {
  name: Link tracklist to gig
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/gigs/{{gigId}}/tracklists/{{tracklistId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 204
}
