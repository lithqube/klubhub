meta {
  name: Unlink tracklist from gig
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/v1/gigs/{{gigId}}/tracklists/{{tracklistId}}
  body: none
  auth: none
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 204
}
