# Optional debugging overlay; default Compose already provides development backends.
# docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d
services:
  storage:
    ports:
      - "127.0.0.1:${GARAGE_ADMIN_PORT:-39002}:3903"
  app:
    environment:
      LOG_LEVEL: debug
