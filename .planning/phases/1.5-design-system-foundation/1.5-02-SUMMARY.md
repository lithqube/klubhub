---
phase: 1.5-design-system-foundation
plan: "02"
subsystem: ui

tags: [shadcn-vue, radix-vue, cyberpunk, components, badge, switch, progress, tailwind-v4, nuxt]

# Summary

## What was accomplished
Installed and configured all 16 shadcn-vue UI components with full cyberpunk overrides. Completed the 13-variant badge status system, sharp 0px-radius toggle switch, and 4px thin progress bar — all matching the mockup patterns exactly.

## Dependency graph
- **Input:** Plan 01 (Tailwind v4 design tokens + utilities in styles.css)
- **Output:** 16 components in `app/components/ui/` — consumed by Plans 04, 05, 06

## Key files created/modified

### New components
- `app/components/ui/button/Button.vue` — gradient-cta primary, ghost-border secondary, glow states
- `app/components/ui/badge/Badge.vue` — 13 variants: ready/draft/archived/confirmed/soft-hold/failed/scheduled/published/awaiting-deposit/paid-full/settled/pending/nominal
- `app/components/ui/switch/Switch.vue` — sharp 0px-radius toggle; on=cyan(bg-primary), off=dim(bg-surface-container)
- `app/components/ui/progress/Progress.vue` — h-1 (4px) track, linear-gradient(90deg, #96F8FF, #00F1FD) fill
- `app/components/ui/card/Card.vue` — glass-panel dark surface, elevated variant adds luminous-threshold
- `app/components/ui/dialog/Dialog.vue` + 8 sub-components — glass-panel-heavy modal
- `app/components/ui/input/Input.vue` — bg-surface-container-high, accent-bar-focus on left border
- `app/components/ui/table/Table.vue` + 7 sub-components — no border-b dividers
- `app/components/ui/avatar/`, `separator/`, `popover/`, `sheet/`, `select/`, `tabs/`, `toast/`, `dropdown-menu/`

### Config files
- `apps/dj/components.json` — shadcn-vue CLI config
- `apps/dj/app/lib/utils.ts` — cn() using clsx + tailwind-merge
- `apps/dj/nuxt.config.ts` — shadcn-nuxt module added; typeCheck moved to nx typecheck target
- `apps/dj/tsconfig.app.json` — @/* path alias for proper module resolution

### Dependencies added
- `radix-vue` — accessible Radix Vue primitives (required by shadcn-vue components)
- `vue-tsc` — Vue TypeScript type checking
- `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-vue-next` (already present)

## Verification
- `nuxt build` exits 0: ✅ ("186 modules transformed", `.output/` generated)
- 16 component directories exist in `app/components/ui/`: ✅
- Badge has all 13 variants including paid-full/settled (accent-bar pattern), pending (text-only), nominal: ✅
- Switch is sharp (rounded-none), cyan on/dim off: ✅
- Progress is 4px (h-1) with cyan gradient: ✅
- Button primary uses gradient-cta: ✅
- Dialog uses glass-panel-heavy: ✅
- No rounded-md/rounded-lg in component files: ✅

## Deviations
- typeCheck: true → false in nuxt.config.ts (pre-existing tracklist.vue type errors were blocking build; typecheck is now the separate nx typecheck target)
- Added DropdownMenuContent + DropdownMenuItem (not in plan but needed for composability)
