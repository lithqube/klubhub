---
phase: 1.5-design-system-foundation
plan: "06"
type: execute
wave: 4
depends_on:
  - 1.5-02
  - 1.5-04
  - 1.5-05
files_modified:
  - apps/dj/app/app.vue
  - apps/dj/app/pages/index.vue
  - apps/dj/app/components/TheNav.vue
  - apps/dj/app/components/TheStatusBar.vue
  - apps/dj/app/components/TheTimelineStrip.vue
autonomous: true
requirements:
  - DSYS-08
  - DSYS-09
  - DSYS-10

must_haves:
  truths:
    - "app.vue uses a fixed 280px sidebar (TheNav.vue) with icon + uppercase text nav items"
    - "TheNav.vue active nav item uses nav-item-active utility (dashed cyan border all 4 sides, not solid fill)"
    - "TheNav.vue inactive nav items have nav-item-inactive (faint dashed underline only)"
    - "TheNav.vue logo area shows 'KlubHub DJ' bold white + 'STATUS: SYNCED' with a green dot below"
    - "app.vue wraps NuxtPage in NuxtErrorBoundary with cyberpunk fallback UI (SYSTEM ERROR heading, glass-panel-heavy)"
    - "ClientOnly Toaster is present in app.vue — wrapping the shadcn-vue Toaster prevents SSR hydration mismatch"
    - "TheStatusBar.vue singleton renders a bottom status bar row with icon + label + value pairs in monospace-feel display"
    - "TheTimelineStrip.vue singleton renders a horizontal scrollable date picker with DD/DAY cells and cyan outline for selected cell"
    - "NxWelcome.vue is deleted"
    - "pages/index.vue redirects to /tracklist"
    - "The app-level background is bg-surface (#0E0E0F)"
    - "Global font defaults are font-data (Inter) and text-on-surface on the root element"
  artifacts:
    - path: "apps/dj/app/app.vue"
      provides: "Global layout — sidebar TheNav + main content area + NuxtErrorBoundary + ClientOnly Toaster + TheStatusBar"
      contains: "NuxtErrorBoundary"
    - path: "apps/dj/app/components/TheNav.vue"
      provides: "Fixed 280px sidebar with logo, nav-item-active/inactive states, module nav items"
      contains: "nav-item-active"
    - path: "apps/dj/app/components/TheStatusBar.vue"
      provides: "Bottom status bar singleton — icon + LABEL + value pairs in monospace display"
      contains: "font-terminal"
    - path: "apps/dj/app/components/TheTimelineStrip.vue"
      provides: "Horizontal scrollable date picker — DD/DAY cells, cyan outline for selected, event dot below"
      contains: "overflow-x-auto"
    - path: "apps/dj/app/pages/index.vue"
      provides: "Redirect to /tracklist"
      contains: "navigateTo"
  key_links:
    - from: "apps/dj/app/app.vue"
      to: "apps/dj/app/components/TheNav.vue"
      via: "sidebar fixed column, TheNav renders nav-item-active/inactive items"
      pattern: "TheNav"
    - from: "apps/dj/app/app.vue"
      to: "apps/dj/app/components/TheStatusBar.vue"
      via: "fixed bottom status bar"
      pattern: "TheStatusBar"
    - from: "apps/dj/app/components/TheNav.vue"
      to: "apps/dj/app/assets/css/styles.css"
      via: "nav-item-active (dashed cyan border) + nav-item-inactive (faint dashed underline)"
      pattern: "nav-item-active"
    - from: "apps/dj/app/app.vue"
      to: "apps/dj/app/components/ui/toast/Toaster.vue"
      via: "<ClientOnly><Toaster /></ClientOnly>"
      pattern: "ClientOnly"
    - from: "apps/dj/app/pages/index.vue"
      to: "apps/dj/app/pages/tracklist.vue"
      via: "navigateTo('/tracklist')"
      pattern: "navigateTo"
---

<objective>
Wire up the complete global app shell: fixed sidebar navigation (TheNav.vue) with dashed active state from the mockup, NuxtErrorBoundary with cyberpunk fallback, ClientOnly Toaster, the bottom status bar singleton (TheStatusBar.vue), the reusable timeline date strip (TheTimelineStrip.vue), and the /tracklist redirect. Delete NxWelcome.vue.

Purpose: This plan completes the application chrome. Every future phase's pages load inside this shell. The sidebar navigation pattern (nav-item-active with dashed cyan border on all 4 sides) and the status bar singleton are concrete mockup patterns required for the Social Scheduler and Gigs modules.
Output: Full cyberpunk app shell with sidebar nav, error boundary, toast, status bar, timeline strip; /tracklist as entry point.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1.5-design-system-foundation/1.5-CONTEXT.md
@.planning/phases/1.5-design-system-foundation/1.5-RESEARCH.md
@.planning/phases/1.5-design-system-foundation/1.5-02-SUMMARY.md

<interfaces>
<!-- NuxtErrorBoundary API (Nuxt 4) -->
<!--
<NuxtErrorBoundary @error="handleError">
  <NuxtPage />
  <template #error="{ error, clearError }">
    <!-- fallback UI using error.message and clearError() callback -->
  </template>
</NuxtErrorBoundary>
-->

<!-- shadcn-vue Toast usage -->
<!-- Toaster: auto-imported from app/components/ui/toast/Toaster.vue -->
<!-- CRITICAL: <Toaster /> MUST be inside <ClientOnly> -->

<!-- nav-item-active utility (from Plan 01 styles.css) -->
<!-- .nav-item-active { border: 1px dashed #96F8FF; @apply text-primary; } -->
<!-- .nav-item-inactive { border-bottom: 1px dashed rgba(150,248,255,0.25); } -->

<!-- TheNav.vue sidebar spec (from mockup) -->
<!--
Width: ~280px fixed
Logo area:
  - "KlubHub DJ" — font-command font-bold text-on-surface (white)
  - "STATUS: SYNCED ●" — font-terminal tracking-terminal text-xs text-tertiary + green dot
Nav items (icon left + uppercase label right):
  - Each item: flex items-center gap-3 px-4 py-3 (48dp touch target)
  - Active:   nav-item-active class (dashed cyan border all 4 sides)
  - Inactive: nav-item-inactive class + text-tertiary hover:text-on-surface
  - Icons from lucide-vue-next: Layers (Tracklist), Calendar (Gigs), Send (Social), etc.
-->

<!-- TheStatusBar.vue spec (from mockup bottom status bar) -->
<!--
Position: fixed bottom-0 left-0 right-0 (full width, always visible)
Background: bg-surface-container-high border-t border-outline-variant/20
Layout: flex items-center gap-8 px-6 py-2
Each item: flex flex-col items-center
  - Small label: font-terminal tracking-terminal text-tertiary text-xs uppercase (e.g. "HARDWARE INTERFACE")
  - Large value: font-command text-on-surface font-semibold (e.g. "1.2ms", "982 MB/s")
  - Optional left icon
Show system stats: API latency, storage speed, sync status, etc.
-->

<!-- TheTimelineStrip.vue spec (from mockup date strip) -->
<!--
Layout: flex overflow-x-auto gap-2 (horizontal scrollable)
Navigation chevrons: < > top-right corner
Each date cell: flex flex-col items-center px-3 py-2 min-w-[48px]
  - Day number: font-command text-sm font-medium
  - Day name: font-terminal text-xs text-tertiary uppercase
  - Selected cell: luminous-threshold (cyan outline outline-1 + glow)
  - Event indicator dot: w-1.5 h-1.5 rounded-full bg-primary BELOW the date number
  - Non-selected: bg-surface-container-low
Props: dates (Date[]), selectedDate (Date), events (Date[])
Emits: update:selectedDate
-->

<!-- Tailwind cyberpunk utilities available -->
<!-- bg-surface, glass-panel-heavy, luminous-threshold -->
<!-- font-command, tracking-command, text-error, text-on-surface, text-tertiary -->
<!-- font-terminal, tracking-terminal -->
<!-- gradient-cta, text-on-primary -->
<!-- nav-item-active, nav-item-inactive (from Plan 01) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TheNav.vue (sidebar with dashed active state), TheStatusBar.vue, and TheTimelineStrip.vue</name>
  <files>apps/dj/app/components/TheNav.vue, apps/dj/app/components/TheStatusBar.vue, apps/dj/app/components/TheTimelineStrip.vue</files>
  <action>
    Create three singleton components.

    **TheNav.vue — sidebar navigation with mockup active state:**
    ```vue
    <script setup lang="ts">
    import { Layers, Calendar, Send, BarChart2, Settings, Bell } from 'lucide-vue-next'

    const route = useRoute()

    const navItems = [
      { label: 'TRACKLIST', icon: Layers, to: '/tracklist' },
      // Future modules added here as phases complete:
      // { label: 'SOCIAL', icon: Send, to: '/social' },
      // { label: 'GIGS', icon: Calendar, to: '/gigs' },
    ]
    </script>

    <template>
      <!-- Fixed sidebar: 280px width, full height -->
      <aside class="w-[280px] min-h-screen bg-surface-container-low flex flex-col flex-shrink-0">
        <!-- Logo area -->
        <div class="px-6 py-5 border-b border-outline-variant/20">
          <p class="font-command font-bold text-on-surface text-lg uppercase tracking-wide">
            KlubHub DJ
          </p>
          <div class="flex items-center gap-2 mt-1">
            <!-- Green status dot -->
            <span class="w-2 h-2 rounded-full bg-green-400" aria-hidden="true" />
            <span class="font-terminal tracking-terminal text-tertiary text-xs uppercase">
              STATUS: SYNCED
            </span>
          </div>
        </div>

        <!-- Nav items -->
        <nav class="flex-1 px-4 py-4 space-y-1">
          <NuxtLink
            v-for="item in navItems"
            :key="item.to"
            :to="item.to"
            class="flex items-center gap-3 px-4 py-3 transition-all duration-200"
            :class="route.path.startsWith(item.to)
              ? 'nav-item-active'
              : 'nav-item-inactive text-tertiary hover:text-on-surface'"
          >
            <component :is="item.icon" class="w-4 h-4 flex-shrink-0" />
            <span class="font-terminal tracking-terminal text-xs uppercase">{{ item.label }}</span>
          </NuxtLink>
        </nav>

        <!-- Bottom: settings + bell -->
        <div class="px-4 py-4 border-t border-outline-variant/20 flex items-center gap-2">
          <button class="flex items-center gap-2 px-3 py-2 text-tertiary hover:text-on-surface transition-colors">
            <Bell class="w-4 h-4" />
          </button>
          <button class="flex items-center gap-2 px-3 py-2 text-tertiary hover:text-on-surface transition-colors">
            <Settings class="w-4 h-4" />
          </button>
        </div>
      </aside>
    </template>
    ```

    **TheStatusBar.vue — bottom status bar singleton:**
    ```vue
    <script setup lang="ts">
    // Status items: hardcoded for Phase 1.5; future phases will pass dynamic data via props or store
    const statusItems = [
      { icon: 'Cpu', label: 'API_LATENCY', value: '-- ms' },
      { icon: 'HardDrive', label: 'STORAGE', value: 'NOMINAL' },
      { icon: 'RefreshCw', label: 'SYNC_STATUS', value: 'IDLE' },
    ]
    </script>

    <template>
      <!-- Fixed bottom bar: full width, always visible -->
      <footer class="fixed bottom-0 left-0 right-0 bg-surface-container-high border-t border-outline-variant/20 z-10">
        <div class="flex items-center gap-8 px-6 py-2">
          <div
            v-for="item in statusItems"
            :key="item.label"
            class="flex flex-col items-center min-w-[80px]"
          >
            <span class="font-terminal tracking-terminal text-tertiary text-xs uppercase">
              {{ item.label }}
            </span>
            <span class="font-command text-on-surface font-semibold text-sm tabular-nums">
              {{ item.value }}
            </span>
          </div>
          <!-- Right-align: version tag -->
          <div class="ml-auto">
            <span class="font-terminal tracking-terminal text-outline text-xs uppercase">
              KLUBHUB_DJ v1.0
            </span>
          </div>
        </div>
      </footer>
    </template>
    ```

    **TheTimelineStrip.vue — horizontal scrollable date picker:**
    ```vue
    <script setup lang="ts">
    import { ChevronLeft, ChevronRight } from 'lucide-vue-next'

    interface Props {
      dates: Date[]
      selectedDate: Date
      events?: Date[]  // dates with event indicators
    }
    const props = withDefaults(defineProps<Props>(), { events: () => [] })
    const emit = defineEmits<{ 'update:selectedDate': [date: Date] }>()

    const dayNames = ['SUN', 'MON', 'TUE', 'WED', 'THU', 'FRI', 'SAT']

    function isSameDay(a: Date, b: Date) {
      return a.toDateString() === b.toDateString()
    }
    function hasEvent(date: Date) {
      return props.events.some(e => isSameDay(e, date))
    }
    </script>

    <template>
      <div class="bg-surface-container-low px-4 py-3">
        <!-- Header: label left + chevrons right -->
        <div class="flex justify-between items-center mb-3">
          <span class="font-terminal tracking-terminal text-tertiary text-xs uppercase">
            TIMELINE_VIEW
          </span>
          <div class="flex gap-1">
            <button class="p-1 text-tertiary hover:text-primary transition-colors">
              <ChevronLeft class="w-4 h-4" />
            </button>
            <button class="p-1 text-tertiary hover:text-primary transition-colors">
              <ChevronRight class="w-4 h-4" />
            </button>
          </div>
        </div>

        <!-- Scrollable date strip -->
        <div class="flex gap-2 overflow-x-auto pb-1 scrollbar-hide">
          <button
            v-for="date in dates"
            :key="date.toISOString()"
            class="flex flex-col items-center min-w-[48px] px-2 py-2 transition-all duration-200 flex-shrink-0"
            :class="isSameDay(date, selectedDate)
              ? 'luminous-threshold bg-surface-container'
              : 'bg-surface-container-low text-tertiary hover:text-on-surface'"
            @click="emit('update:selectedDate', date)"
          >
            <!-- Day number -->
            <span
              class="font-command text-sm font-medium"
              :class="isSameDay(date, selectedDate) ? 'text-primary' : ''"
            >
              {{ date.getDate() }}
            </span>
            <!-- Day name -->
            <span class="font-terminal tracking-terminal text-xs uppercase mt-0.5">
              {{ dayNames[date.getDay()] }}
            </span>
            <!-- Event indicator dot (below date) -->
            <span
              v-if="hasEvent(date)"
              class="w-1.5 h-1.5 rounded-full bg-primary mt-1"
              aria-label="has event"
            />
            <span v-else class="w-1.5 h-1.5 mt-1" />
          </button>
        </div>
      </div>
    </template>
    ```

    Note: TheTimelineStrip.vue is built and available for Phase 2+ modules (Social Scheduler has timeline views). It is not wired into any page in this phase — it is created as a singleton component ready for use.
  </action>
  <verify>
    <automated>cd /Users/admin/dev/klubhub-dj && pnpm nx build @dev/dj 2>&1 | tail -10</automated>
  </verify>
  <done>Three singleton components created in apps/dj/app/components/; build succeeds; TheNav uses nav-item-active utility; TheStatusBar has font-terminal labels + font-command values; TheTimelineStrip has overflow-x-auto scroll and luminous-threshold for selected cell</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite app.vue with sidebar shell + error boundary + status bar; replace index.vue; delete NxWelcome.vue</name>
  <files>apps/dj/app/app.vue, apps/dj/app/pages/index.vue, apps/dj/app/components/NxWelcome.vue</files>
  <action>
    **Replace apps/dj/app/app.vue with full sidebar layout:**

    ```vue
    <script setup lang="ts">
    function handleError(error: Error) {
      console.error('[KlubHub] Unhandled error:', error)
    }
    </script>

    <template>
      <div class="bg-surface min-h-screen font-data text-on-surface flex">
        <!-- Sidebar navigation (fixed 280px) -->
        <TheNav />

        <!-- Main content area: grows to fill remaining width, padded bottom for status bar -->
        <div class="flex-1 flex flex-col min-h-screen pb-10">
          <!-- Page content with error boundary -->
          <NuxtErrorBoundary @error="handleError">
            <NuxtPage />
            <template #error="{ error, clearError }">
              <div class="flex items-center justify-center min-h-[60vh] p-8">
                <div class="glass-panel-heavy luminous-threshold p-8 max-w-lg w-full space-y-4">
                  <h1 class="font-command tracking-command text-error text-2xl font-bold uppercase">
                    SYSTEM ERROR
                  </h1>
                  <p class="font-terminal tracking-terminal text-on-surface-variant text-xs uppercase leading-relaxed">
                    {{ error.message || 'An unexpected error occurred. Please reinitialize.' }}
                  </p>
                  <button
                    class="gradient-cta text-on-primary px-6 py-3 font-terminal tracking-terminal text-xs uppercase font-semibold w-full hover:shadow-glow-primary-strong transition-shadow duration-200"
                    @click="clearError()"
                  >
                    REINITIALIZE
                  </button>
                </div>
              </div>
            </template>
          </NuxtErrorBoundary>
        </div>

        <!-- Toast notification system — ClientOnly prevents SSR hydration mismatch -->
        <ClientOnly>
          <Toaster />
        </ClientOnly>

        <!-- Bottom status bar (fixed, full width) -->
        <TheStatusBar />
      </div>
    </template>
    ```

    Layout notes:
    - `flex` on root + `TheNav` (fixed 280px) + `flex-1` main area = sidebar + content layout
    - `pb-10` on main area ensures content is not obscured by the fixed status bar (~40px)
    - TheNav is the full sidebar from Task 1; future nav items added there as modules ship
    - TheStatusBar is fixed to the bottom of the viewport (z-10) from Task 1

    **Replace apps/dj/app/pages/index.vue:**
    ```vue
    <script setup lang="ts">
    // Redirect to /tracklist as the primary entry point of the app
    await navigateTo('/tracklist', { replace: true })
    </script>

    <template>
      <!-- Redirecting to /tracklist -->
    </template>
    ```

    **Delete apps/dj/app/components/NxWelcome.vue:**
    ```bash
    rm apps/dj/app/components/NxWelcome.vue
    ```

    Run build and e2e to confirm nothing broke:
    ```
    pnpm nx build @dev/dj && pnpm nx e2e dj-e2e
    ```

    If e2e tests check for NxWelcome content, update those assertions to check for the sidebar or tracklist page instead.
  </action>
  <verify>
    <automated>ls /Users/admin/dev/klubhub-dj/apps/dj/app/components/NxWelcome.vue 2>&1; cd /Users/admin/dev/klubhub-dj && pnpm nx build @dev/dj 2>&1 | tail -10</automated>
  </verify>
  <done>NxWelcome.vue does not exist (ls returns error); app.vue uses TheNav sidebar layout; index.vue contains navigateTo('/tracklist'); build succeeds</done>
</task>

</tasks>

<verification>
After all tasks:

1. `pnpm nx build @dev/dj` exits 0
2. `ls apps/dj/app/components/NxWelcome.vue` — file not found
3. `grep "NuxtErrorBoundary" apps/dj/app/app.vue` — error boundary present
4. `grep "ClientOnly" apps/dj/app/app.vue` — Toaster wrapped
5. `grep "TheStatusBar" apps/dj/app/app.vue` — status bar wired
6. `grep "TheNav" apps/dj/app/app.vue` — sidebar nav wired
7. `grep "nav-item-active" apps/dj/app/components/TheNav.vue` — active state class used
8. `grep "REALTIME_OUTPUT_PREVIEW\|LIVE_RENDER" apps/dj/app/components/TheNav.vue` — NOT present (these are in TrackcardPreviewPanel)
9. `grep "navigateTo" apps/dj/app/pages/index.vue` — redirect confirmed
10. `pnpm nx e2e dj-e2e` — all tests pass

Manual check during dev: visiting `http://localhost:4200` should redirect to `/tracklist` and show the 280px sidebar with "KlubHub DJ" + "STATUS: SYNCED" logo area.
</verification>

<success_criteria>
- `pnpm nx build @dev/dj` exits 0
- `pnpm nx e2e dj-e2e` passes
- app.vue uses sidebar layout (TheNav + flex-1 main + TheStatusBar)
- TheNav.vue: 280px sidebar, nav-item-active uses dashed cyan border all 4 sides, inactive uses faint dashed underline, logo area has "KlubHub DJ" + "STATUS: SYNCED ●"
- TheStatusBar.vue: fixed bottom bar with font-terminal labels + font-command values + tabular-nums
- TheTimelineStrip.vue: horizontal scrollable, overflow-x-auto, luminous-threshold for selected cell, event dot below date
- app.vue contains NuxtErrorBoundary with cyberpunk error fallback
- app.vue contains `<ClientOnly><Toaster /></ClientOnly>`
- NxWelcome.vue no longer exists
- index.vue redirects to /tracklist with `replace: true`
- No scoped CSS in app.vue or any The*.vue singleton (Tailwind classes only)
</success_criteria>

<output>
After completion, create `.planning/phases/1.5-design-system-foundation/1.5-06-SUMMARY.md`
</output>
