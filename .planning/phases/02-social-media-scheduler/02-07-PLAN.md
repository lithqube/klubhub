---
phase: 02-social-media-scheduler
plan: "07"
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-04
  - 02-05
  - 02-06
autonomous: false
files_modified: []
requirements:
  - SOCL-01
  - SOCL-02
  - SOCL-03
  - SOCL-04
  - SOCL-05
  - SOCL-06
  - SOCL-07
  - SOCL-08
  - SOCL-09
  - SOCL-10
  - SOCL-11
  - SOCL-12
  - SOCL-13
  - SOCL-14
  - SOCL-15
  - SOCL-16
  - SOCL-17

must_haves:
  truths:
    - "All automated tests pass across backend and frontend"
    - "User can navigate to /social and see the queue and calendar tabs"
    - "OAuth connection flow works end-to-end in browser"
    - "Scheduling a post appears in the queue with correct status and datetime"
    - "The Schedule to Instagram CTA on the tracklist page navigates to the social page with image pre-filled"
  artifacts:
    - path: "apps/dj/app/pages/social.vue"
      provides: "Full social scheduler page accessible at /social"
    - path: "api/internal/social/"
      provides: "Complete Go social package with worker running"
  key_links:
    - from: "apps/dj/app/components/tracklist/TracklistExporter.vue"
      to: "apps/dj/app/pages/social.vue"
      via: "?imageId navigation"
      pattern: "imageId"
---

<objective>
Human verification checkpoint for the complete social media scheduler feature. Confirm all automated tests pass, all visual states render correctly, and the OAuth flow works in the browser.

Purpose: The social scheduler has multiple UI states (connected/disconnected, failed/published/scheduled cards, calendar dots, compose panel pre-fill) that require visual and interactive confirmation. This checkpoint gates phase completion.
Output: User confirms feature works end-to-end; phase complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-social-media-scheduler/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite green check</name>
  <files>
    api/internal/social/handler_test.go,
    api/internal/social/service_test.go,
    api/internal/social/repository_test.go,
    apps/dj/app/components/social/__tests__/SocialPostCompose.test.ts,
    apps/dj/app/components/social/__tests__/SocialQueueGrid.test.ts,
    apps/dj/app/components/social/__tests__/SocialTokenWarningBanner.test.ts,
    apps/dj/app/components/social/__tests__/SocialCalendarView.test.ts
  </files>
  <action>
    Run the complete test suite for both backend and frontend social modules:
    1. Backend: `pnpm nx test api -- -count=1 ./internal/social/...`
    2. Frontend: `pnpm nx test dj -- --testPathPattern social`
    3. Full suite: `pnpm nx run-many -t test -p dj api`

    If any tests fail, fix them before proceeding to the checkpoint.
    Report test counts: X passed, 0 failed for each.
  </action>
  <verify>
    <automated>cd /Users/admin/dev/klubhub-dj && pnpm nx run-many -t test -p dj api 2>&1 | tail -30</automated>
  </verify>
  <done>All social tests GREEN; zero failing tests in both dj and api packages</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of social scheduler UI and flows</name>
  <action>Present verification steps to user and wait for approval signal.</action>
  <what-built>
    Complete Social Media Scheduler — Phase 2:
    - Go backend: social package (handler/service/repository/worker/instagram client), migration 003, routes registered under /api/v1/social/
    - Background publishing worker polling every 60s with exponential backoff retry (5→20→80 min), rate limit enforcement, duplicate prevention, token refresh
    - Pinia social store (useSocialStore) + useSocialPostForm composable
    - social.vue page with Queue and Calendar tabs
    - SocialPostCompose: collapsible compose panel with caption auto-gen, char counter, timezone combobox, datetime picker
    - SocialQueueGrid: 3-column card grid with SocialPostCard (inline expand-to-edit), SocialPostCardFailed (magenta banner + RETRY SYNC + DOWNLOAD IMAGE), SocialNextSlotCard (dashed empty card)
    - SocialCalendarView: monthly grid with status-colored dot indicators, day-click filtering
    - SocialConnectionBanner + SocialTokenWarningBanner (orange 7-day + magenta disconnected)
    - TheNav: Social entry added
    - TracklistExporter: "SCHEDULE TO INSTAGRAM" CTA navigates to /social?imageId={id}
  </what-built>
  <how-to-verify>
    **Step 1 — App starts cleanly:**
    Run `docker compose up -d` and confirm all services healthy. Check logs: `docker compose logs api | grep -i social` — should show "publish worker started".

    **Step 2 — Social page loads:**
    Navigate to http://localhost:3000/social. Verify:
    - Page header shows "SOCIAL SCHEDULER" in cyan italic
    - QUEUE and CALENDAR tabs are present; ANALYTICS and AUTOMATIONS are labeled (not clickable or show "Coming Soon")
    - "CONNECT INSTAGRAM" banner appears (since no account connected)
    - "NEXT SLOT" dashed card appears in the empty queue
    - "+ ADD TO QUEUE" and "⚡ QUICK EXPORT FROM TRACKLIST" buttons in top-right

    **Step 3 — Compose panel:**
    Click "+ ADD TO QUEUE". Verify:
    - Compose panel expands inline above the queue
    - Feed/Story toggle works; caption counter shows "0 / 2200" for Feed
    - Switch to Story: counter dims and shows "Captions are not shown on Stories"
    - Timezone combobox is searchable (type "Berlin" → shows "Europe/Berlin")
    - Submit button is disabled when required fields are empty

    **Step 4 — Calendar tab:**
    Click CALENDAR tab. Verify:
    - Monthly grid renders with correct day layout
    - Prev/Next month navigation works
    - (No posts yet, so no dots — that's expected)

    **Step 5 — Tracklist → Social flow (SOCL-17):**
    If a previous tracklist export exists: navigate to /tracklist, find an exported image, and click the export button to re-export if needed. Verify:
    - "SCHEDULE TO INSTAGRAM" button appears in the export result area
    - Clicking it navigates to /social?imageId=...
    - Compose panel opens automatically
    - Caption is auto-filled with tracklist metadata (DJ name, track count, BPM)

    **Step 6 — OAuth URL (SOCL-01):**
    Click "CONNECT INSTAGRAM". Verify:
    - Browser attempts to navigate to an Instagram OAuth URL (will likely redirect to Meta's auth page or fail with "app not verified" in dev mode — that's expected)
    - Confirm the URL contains: client_id, redirect_uri, scope=instagram_business_basic,instagram_business_content_publish

    **Step 7 — API health (SOCL routes registered):**
    Run: `curl -s http://localhost:8080/api/v1/social/accounts | jq .`
    Expected: `{"data": null}` (no account connected)

    Run: `curl -s "http://localhost:8080/api/v1/social/auth/url" | jq .`
    Expected: `{"url": "https://api.instagram.com/oauth/authorize?..."}` (valid OAuth URL)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
pnpm nx run-many -t test -p dj api 2>&1 | tail -20
</verification>

<success_criteria>
- All automated tests GREEN (zero failures)
- Social page loads and renders all UI states correctly
- Compose panel works with caption auto-gen and char counter
- Calendar view renders monthly grid
- Tracklist→Social navigation flow works with image pre-fill
- API routes respond correctly
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-social-media-scheduler/02-07-SUMMARY.md`
</output>
