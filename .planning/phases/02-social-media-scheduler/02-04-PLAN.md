---
phase: 02-social-media-scheduler
plan: "04"
type: execute
wave: 2
depends_on:
  - 02-03
files_modified:
  - apps/dj/app/pages/social.vue
  - apps/dj/app/components/social/SocialPageHeader.vue
  - apps/dj/app/components/social/SocialPostCompose.vue
  - apps/dj/app/components/social/SocialQueueGrid.vue
  - apps/dj/app/components/social/SocialPostCard.vue
  - apps/dj/app/components/social/SocialPostCardFailed.vue
  - apps/dj/app/components/social/SocialNextSlotCard.vue
  - apps/dj/app/components/social/SocialTabs.vue
  - apps/dj/app/components/social/__tests__/SocialPostCompose.test.ts
autonomous: true
requirements:
  - SOCL-05
  - SOCL-06
  - SOCL-07
  - SOCL-08
  - SOCL-09
  - SOCL-12
  - SOCL-14
  - SOCL-17

must_haves:
  truths:
    - "social.vue page mounts, calls loadPosts() and loadAccount(), renders queue grid"
    - "SocialPostCompose panel is collapsed by default; expands when composePanelOpen is true or when ?imageId query param is present"
    - "SocialPostCompose shows Feed/Story toggle; caption textarea with character counter; datetime-local + timezone combobox"
    - "Caption counter shows '482 / 2200' for feed and dim note for story per CONTEXT decisions"
    - "SocialQueueGrid renders 3-column grid of SocialPostCard components"
    - "SocialPostCard shows image thumbnail, status badge, datetime, caption excerpt, platform tag (INSTAGRAM / INSTAGRAM REELS)"
    - "SocialPostCard expands inline on click when status=scheduled; shows editable fields; collapses on save"
    - "SocialPostCardFailed shows magenta banner with ATTENTION REQUIRED text, RETRY SYNC and DOWNLOAD IMAGE buttons"
    - "SocialNextSlotCard shows dashed border empty card when queue is empty"
    - "SocialTabs shows QUEUE (active) and CALENDAR tabs; ANALYTICS and AUTOMATIONS are stub labels with coming-soon tooltip"
  artifacts:
    - path: "apps/dj/app/pages/social.vue"
      provides: "Thin layout page; reads ?imageId query; wires SocialPageHeader + SocialTabs + SocialConnectionBanner + SocialPostCompose + queue/calendar views"
    - path: "apps/dj/app/components/social/SocialPostCompose.vue"
      provides: "Collapsible compose panel with caption, post type, timezone, datetime; emits submit with CreatePostRequest"
    - path: "apps/dj/app/components/social/SocialPostCard.vue"
      provides: "Queue card with inline expand-to-edit for scheduled status"
    - path: "apps/dj/app/components/social/SocialPostCardFailed.vue"
      provides: "Failed post card with RETRY SYNC + DOWNLOAD IMAGE actions"
    - path: "apps/dj/app/components/social/SocialQueueGrid.vue"
      provides: "3-column card grid with SocialNextSlotCard at end"
  key_links:
    - from: "apps/dj/app/pages/social.vue"
      to: "apps/dj/app/stores/social.ts"
      via: "useSocialStore() + storeToRefs()"
      pattern: "useSocialStore"
    - from: "apps/dj/app/components/social/SocialPostCompose.vue"
      to: "apps/dj/app/composables/useSocialPostForm.ts"
      via: "useSocialPostForm() composable"
      pattern: "useSocialPostForm"
    - from: "apps/dj/app/pages/social.vue"
      to: "?imageId query param"
      via: "useRoute().query.imageId → store.openComposePanel(imageId)"
      pattern: "imageId"
---

<objective>
Build the social scheduler frontend: the social.vue page, compose panel, queue grid, post cards, and tab navigation. This plan implements the visual layer of the scheduler using the store and composables from Plan 02-03.

Purpose: This is what the user sees and interacts with — the core social scheduler UI. All locked CONTEXT decisions (card layout, inline editing, magenta failed banners, collapsed compose panel, "NEXT SLOT" dashed card) are implemented here.
Output: Fully functional queue view with compose panel, post cards in all statuses, and tab navigation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-social-media-scheduler/02-CONTEXT.md
@.planning/phases/02-social-media-scheduler/02-RESEARCH.md
@.planning/phases/02-social-media-scheduler/02-03-SUMMARY.md
</context>

<interfaces>
<!-- Types and store shape from plan 02-03. -->

From apps/dj/app/types/social.ts:
```typescript
export type PostStatus = 'draft' | 'scheduled' | 'publishing' | 'published' | 'failed' | 'permanently_failed'
export type PostType = 'feed' | 'story'

export interface SocialAccount {
  id: string
  accountName: string
  status: 'connected' | 'disconnected'
  tokenExpiry: string | null
}

export interface ScheduledPost {
  id: string
  status: PostStatus
  postType: PostType
  caption: string
  imageMinioPath: string
  scheduledAtUtc: string
  timezoneName: string
  retryCount: number
  lastError: string
}
```

From apps/dj/app/stores/social.ts (useSocialStore):
```typescript
const { posts, account, loading, composePanelOpen, prefilledImageId } = storeToRefs(store)
store.loadPosts()
store.loadAccount()
store.createPost(req)
store.editPost(id, req)
store.retryPost(id)
store.downloadImage(id)
store.openComposePanel(imageId?)
store.closeComposePanel()
```

From apps/dj/app/composables/useSocialPostForm.ts:
```typescript
const { postType, caption, scheduledAt, timezoneName, timezoneSearch, imageId,
        charLimit, charCount, isOverLimit, timezoneOptions, scheduledAtUTCPreview,
        generateCaption, resetForm } = useSocialPostForm()
```

Existing shadcn-vue components available (already installed):
- ui/badge/Badge.vue — variants: default, secondary, destructive + custom (use inline class overrides for cyberpunk variants)
- ui/card/Card.vue — glass-panel dark surface
- ui/input/Input.vue — accent-bar-focus
- ui/select/Select.vue — for timezone (or build combobox with popover + input)
- ui/tabs/Tabs.vue + TabsList + TabsTrigger + TabsContent — for QUEUE/CALENDAR
- ui/switch/Switch.vue — Feed/Story toggle
- ui/separator/Separator.vue

Design token classes from styles.css:
- glass-panel — backdrop-blur glassmorphism surface
- gradient-cta — gradient button (primary actions like "+ ADD TO QUEUE")
- ghost-border — ghost variant border button ("⚡ QUICK EXPORT FROM TRACKLIST")
- Status colors: text-primary (#96F8FF cyan), text-secondary (#FF2D78 magenta), text-on-surface-dim (gray)
- bg-surface, bg-surface-container, bg-surface-variant
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: social.vue page + SocialPageHeader + SocialTabs</name>
  <files>
    apps/dj/app/pages/social.vue,
    apps/dj/app/components/social/SocialPageHeader.vue,
    apps/dj/app/components/social/SocialTabs.vue
  </files>
  <action>
    **social.vue** (thin layout, ~60 lines max):
    - `useRoute().query.imageId` — on mount, if imageId query param present: call `store.openComposePanel(imageId as string)`
    - Call `store.loadPosts()` and `store.loadAccount()` on mount (in onMounted or using useAsyncData)
    - Layout: full-width flex-col. Stack from top: SocialPageHeader → SocialConnectionBanner (if !account or account.status === 'disconnected') → SocialTokenWarningBanner (if account and token near expiry) → SocialTabs (with queue/calendar slot content)
    - activeTab ref controls QUEUE vs CALENDAR display
    - Pass `posts` and `account` down as props or let child components access store directly

    **SocialPageHeader.vue**:
    - Props: `account: SocialAccount | null`
    - Left: "SOCIAL SCHEDULER" in font-display italic text-primary (cyan). Below: breadcrumb "STUDIO / SOCIAL"
    - Right: if account connected → green status dot (w-2 h-2 rounded-full bg-emerald-400) + "@{accountName}" text + "DISCONNECT" ghost button. If disconnected → nothing (banner below will show CTA).
    - Use Space Grotesk font-display class for heading

    **SocialTabs.vue**:
    - Uses shadcn-vue Tabs component
    - Tabs: "QUEUE" (active by default, underlined with cyan) and "CALENDAR"
    - "ANALYTICS" and "AUTOMATIONS" rendered as disabled tab-like labels (not real TabsTrigger) with a tooltip "Coming Soon" on hover — per CONTEXT decision (stub, no backend)
    - Right side of tab bar: `⚡ QUICK EXPORT FROM TRACKLIST` (ghost-border button, emits 'quick-export') + `+ ADD TO QUEUE` (gradient-cta button, emits 'add-to-queue')
    - Emits: 'tab-change' (value: 'queue' | 'calendar'), 'quick-export', 'add-to-queue'
    - The active tab underline should use border-b-2 border-primary (cyan)
  </action>
  <verify>
    <automated>cd /Users/admin/dev/klubhub-dj && pnpm nx build dj 2>&1 | tail -20</automated>
  </verify>
  <done>social.vue page renders without error; page mounts and calls loadPosts/loadAccount; SocialTabs shows correct tab layout with stub Analytics/Automations</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SocialPostCompose + SocialQueueGrid + SocialPostCard + SocialPostCardFailed + SocialNextSlotCard</name>
  <files>
    apps/dj/app/components/social/SocialPostCompose.vue,
    apps/dj/app/components/social/SocialQueueGrid.vue,
    apps/dj/app/components/social/SocialPostCard.vue,
    apps/dj/app/components/social/SocialPostCardFailed.vue,
    apps/dj/app/components/social/SocialNextSlotCard.vue,
    apps/dj/app/components/social/__tests__/SocialPostCompose.test.ts
  </files>
  <behavior>
    - SocialPostCompose: caption auto-gen runs when prefilledImageId prop changes from null to a value
    - SocialPostCompose: charCounter shows "482 / 2200" for feed; shows dim note "Captions are not shown on Stories" for story
    - SocialPostCompose: isOverLimit adds red ring / text-destructive class to caption textarea
    - SocialPostCompose: Submit calls store.createPost() then store.closeComposePanel() then store.loadPosts()
    - SocialPostCard: click on scheduled card toggles expanded state; non-scheduled cards are not expandable
    - SocialPostCardFailed: RETRY SYNC calls store.retryPost(id); DOWNLOAD IMAGE calls store.downloadImage(id)
  </behavior>
  <action>
    **SocialPostCompose.vue**:
    - Props: `prefilledImageId: string | null`
    - Uses `useSocialPostForm()` composable for all form state
    - Watch `prefilledImageId` prop: when it changes to a non-null value, call `generateCaption(tracklist)` if tracklist store has data (import useTracklistStore)
    - Layout (collapsible panel, shown when composePanelOpen === true):
      - First row: Feed/Story toggle (Switch component) + post type label
      - Caption textarea (Input/textarea with accent-bar-focus): multiline, shows charCount/charLimit for feed, shows "Captions are not shown on Stories" dim note for story
      - Image attachment: if prefilledImageId is set, show thumbnail with "ATTACHED FROM TRACKLIST" label + × remove button; else show "Upload Image" file input
      - Datetime picker + timezone combobox (side by side): datetime-local input + searchable combobox built from popover + input + list of timezoneOptions filtered by timezoneSearch; show scheduledAtUTCPreview below
      - Submit button: gradient-cta "+ SCHEDULE POST"; disabled when caption empty or scheduledAt empty or imageId/imageFile not set or isOverLimit
    - On submit: call store.createPost({...}), then store.closeComposePanel(), then resetForm(), then store.loadPosts()

    **SocialQueueGrid.vue**:
    - Props: `posts: ScheduledPost[]`
    - 3-column grid (grid-cols-3 gap-4)
    - For each post: render SocialPostCardFailed if status === 'failed' or 'permanently_failed', else render SocialPostCard
    - Always append SocialNextSlotCard at the end of the grid

    **SocialPostCard.vue**:
    - Props: `post: ScheduledPost`
    - Card structure (glass-panel):
      - Top: image thumbnail (img with src from MinIO via API image URL, or placeholder if no image) — fixed height 160px, object-cover
      - Status badge: use Badge component. Status → badge variant mapping: scheduled='READY' (cyan filled), publishing='PUBLISHING' (cyan pulse animation), published='PUBLISHED' (gray), draft='DRAFT' (outline)
      - Datetime: formatted as "OCT 24, 2026 — 23:45" (uppercase, monospace-ish)
      - Caption excerpt: truncated to 2 lines (line-clamp-2)
      - Platform tag at bottom: "INSTAGRAM" (feed) or "INSTAGRAM REELS" (story) — small uppercase ghost-border label
    - Click handler: if post.status === 'scheduled', toggle `expanded` ref
    - Expanded state shows editable fields inline (caption textarea, datetime, timezone) + "SAVE" gradient-cta button + "CANCEL" ghost button
    - On SAVE: call store.editPost(post.id, { caption, scheduledAt, timezoneName }) then collapse
    - Edit blocked visually on non-scheduled posts (no click handler, cursor-default)

    **SocialPostCardFailed.vue**:
    - Props: `post: ScheduledPost`
    - Full-width magenta bar at top: `⚠ ATTENTION REQUIRED: SYNC ERROR` (bg-secondary/20 border-b border-secondary text-secondary)
    - Same card structure as SocialPostCard (no expand-to-edit)
    - Status badge: FAILED (magenta filled)
    - Error reason text below status: `post.lastError` in text-secondary/70 text-sm
    - Footer row: "RETRY SYNC" (text-secondary ghost button) + "DOWNLOAD IMAGE" (text-primary ghost button)
    - RETRY SYNC calls store.retryPost(post.id); DOWNLOAD IMAGE calls store.downloadImage(post.id)

    **SocialNextSlotCard.vue**:
    - Dashed border card (border-2 border-dashed border-surface-variant, bg-transparent)
    - Shows a future datetime slot — compute next whole hour from now + 1 hour
    - "SCHEDULE NEW ACTIVITY" label in text-on-surface-dim uppercase at bottom
    - Clicking it calls store.openComposePanel()

    **SocialPostCompose.test.ts**: Test caption auto-gen runs when prefilledImageId changes. Test char counter shows "N / 2200" for feed. Test charLimit is null for story and note text is visible. Test submit is disabled when required fields empty.
  </action>
  <verify>
    <automated>cd /Users/admin/dev/klubhub-dj && pnpm nx test dj -- --testPathPattern SocialPostCompose 2>&1 | tail -20</automated>
  </verify>
  <done>SocialPostCompose tests GREEN; all social components build without TypeScript errors; queue grid renders with 3-column layout</done>
</task>

</tasks>

<verification>
cd /Users/admin/dev/klubhub-dj && pnpm nx build dj 2>&1 | tail -20
</verification>

<success_criteria>
- All social components build without TypeScript errors
- SocialPostCompose tests GREEN (caption auto-gen, char counter, submit guard)
- social.vue page opens and loads data on mount
- ?imageId query param triggers compose panel open
- Failed post cards show magenta banner with correct actions
- Queue grid renders correct column layout
</success_criteria>

<output>
After completion, create `.planning/phases/02-social-media-scheduler/02-04-SUMMARY.md`
</output>
