---
phase: 02-social-media-scheduler
plan: "06"
type: execute
wave: 3
depends_on:
  - 02-04
  - 02-05
files_modified:
  - apps/dj/app/components/tracklist/TracklistExporter.vue
  - apps/dj/app/pages/social.vue
autonomous: true
requirements:
  - SOCL-17

must_haves:
  truths:
    - "TracklistExporter.vue shows a 'Schedule to Instagram' button after a successful export"
    - "Clicking 'Schedule to Instagram' navigates to /social?imageId={minioImageId}"
    - "social.vue page reads ?imageId on mount and calls store.openComposePanel(imageId)"
    - "Compose panel auto-fills the tracklist image as the attachment"
    - "Caption auto-generation uses the tracklist store data (DJ name, title, track count, avg BPM)"
  artifacts:
    - path: "apps/dj/app/components/tracklist/TracklistExporter.vue"
      provides: "Schedule to Instagram CTA after export — navigates to /social?imageId={id}"
  key_links:
    - from: "apps/dj/app/components/tracklist/TracklistExporter.vue"
      to: "apps/dj/app/pages/social.vue"
      via: "navigateTo('/social?imageId=' + imageId)"
      pattern: "social.*imageId"
    - from: "apps/dj/app/pages/social.vue"
      to: "apps/dj/app/stores/social.ts"
      via: "store.openComposePanel(imageId) triggered on mount when query param present"
      pattern: "openComposePanel"
---

<objective>
Wire the "Schedule Post" call-to-action from the Tracklist Exporter to the Social page, completing the end-to-end flow from image export to post scheduling.

Purpose: SOCL-17 is the product's key workflow integration — a DJ exports their tracklist image and with one click lands on the Social page with the image pre-attached and caption auto-generated. This plan implements that bridge.
Output: TracklistExporter shows "Schedule to Instagram" button post-export; social.vue reads imageId param and pre-fills the compose panel.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-social-media-scheduler/02-CONTEXT.md
@.planning/phases/02-social-media-scheduler/02-04-SUMMARY.md
@.planning/phases/02-social-media-scheduler/02-05-SUMMARY.md
</context>

<interfaces>
<!-- Key patterns for the wiring task. -->

From social.vue (created in plan 02-04) — existing onMounted to extend:
```vue
onMounted(async () => {
  await Promise.all([store.loadPosts(), store.loadAccount()])
  // Add: check query.imageId here
})
```

From useSocialStore:
```typescript
store.openComposePanel(imageId?: string): void
// sets composePanelOpen = true, prefilledImageId = imageId
```

From TracklistExporter.vue — existing export result handling:
- After successful export, the component receives a MinIO image path/ID in the response
- Currently this result is only shown as a download link
- Add a "SCHEDULE TO INSTAGRAM" ghost-border button next to the existing export result UI

From CONTEXT.md decisions (locked):
- "Flow: Navigate to Social page — clicking 'Schedule to Instagram' on the tracklist page navigates to /social?imageId={id}"
- Navigation (not a sheet/modal) so the user sees their queue context while composing
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: TracklistExporter "Schedule to Instagram" CTA + social.vue imageId wiring</name>
  <files>
    apps/dj/app/components/tracklist/TracklistExporter.vue,
    apps/dj/app/pages/social.vue
  </files>
  <action>
    **TracklistExporter.vue** — read the file first to understand existing export result state:
    - Find where the export success state is tracked (likely a `exportResult` or `exportedImagePath` ref or similar)
    - After a successful export, the response from the API contains the MinIO image path(s). The Story image ID and Square image ID are the values to pass.
    - Add a "SCHEDULE TO INSTAGRAM" button (ghost-border class) in the export result area. Place it beneath or beside the existing download button(s).
    - On click: call `navigateTo('/social?imageId=' + encodeURIComponent(storyImagePath))` — use the Story format (1080×1920) as the default since Stories are the primary DJ post format. If only square was exported, use square's path.
    - The button only appears AFTER a successful export (conditionally rendered on the same state flag as the download button). Do NOT show it before export completes.
    - Use `useRouter()` or Nuxt's `navigateTo()` for navigation (not `window.location.href`).
    - Label: "⚡ SCHEDULE TO INSTAGRAM" (ghost-border, text-primary, uppercase) — matches the "QUICK EXPORT FROM TRACKLIST" button style in the Social page

    **social.vue** — ensure imageId wiring is complete (may already be partially implemented in plan 02-04, verify):
    - In onMounted: `const imageId = useRoute().query.imageId as string | undefined`; if imageId is truthy: `store.openComposePanel(imageId)`
    - This triggers composePanelOpen = true AND prefilledImageId = imageId in the store
    - SocialPostCompose watches prefilledImageId (from store) and triggers caption auto-generation
    - The compose panel should scroll into view when opened via imageId: after openComposePanel(), use `nextTick(() => document.querySelector('[data-compose-panel]')?.scrollIntoView({ behavior: 'smooth' }))`
    - Add `data-compose-panel` attribute to the SocialPostCompose root element for the scroll target
  </action>
  <verify>
    <automated>cd /Users/admin/dev/klubhub-dj && pnpm nx build dj 2>&1 | tail -20</automated>
  </verify>
  <done>TracklistExporter shows "SCHEDULE TO INSTAGRAM" button after export; clicking it navigates to /social?imageId=...; social.vue opens compose panel and pre-fills image when imageId param is present; build passes with no TypeScript errors</done>
</task>

</tasks>

<verification>
cd /Users/admin/dev/klubhub-dj && pnpm nx build dj 2>&1 | tail -10
</verification>

<success_criteria>
- TracklistExporter renders "SCHEDULE TO INSTAGRAM" button after successful export
- Navigation takes user to /social?imageId={path}
- social.vue opens compose panel automatically when imageId query param is present
- Caption auto-generation fires with tracklist metadata
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-social-media-scheduler/02-06-SUMMARY.md`
</output>
