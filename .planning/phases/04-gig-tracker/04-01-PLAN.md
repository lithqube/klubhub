---
phase: 04-gig-tracker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/internal/platform/migrations/005_gigs.sql
  - api/internal/platform/migrations/005b_venues.sql
  - api/internal/platform/migrations/005c_contacts.sql
  - api/internal/platform/migrations/005d_gig_venues.sql
  - api/internal/platform/migrations/005e_gig_contacts.sql
  - api/internal/platform/migrations/005f_tracklist_gigs.sql
  - api/internal/gig/model.go
  - api/internal/gig/repository.go
  - api/internal/gig/service.go
  - api/internal/gig/handler.go
  - api/internal/venue/model.go
  - api/internal/venue/repository.go
  - api/internal/venue/service.go
  - api/internal/venue/handler.go
  - api/internal/contact/model.go
  - api/internal/contact/repository.go
  - api/internal/contact/service.go
  - api/internal/contact/handler.go
  - api/internal/router.go
autonomous: true
requirements:
  - GIG-01
  - GIG-02
  - GIG-03
  - GIG-04
  - GIG-09
  - GIG-10
  - CONT-01
  - CONT-02
  - CONT-06
must_haves:
  truths:
    - "Gig status transitions are enforced: inquiry→confirmed→advanced→played→cancelled (cancelled is terminal)"
    - "Payment status tracked independently: unpaid, deposit_paid, paid, overdue, waived"
    - "Venue and Contact records are reusable across gigs"
    - "GigReader interface is stable for downstream modules (Finance, Tour, EPK)"
    - "All entities use soft deletion with deleted_at timestamp"
  artifacts:
    - path: api/internal/gig/model.go
      provides: Gig, GigCreate, GigUpdate, GigStatus, PaymentStatus, GigReader interface
      min_lines: 80
    - path: api/internal/gig/repository.go
      provides: CRUD operations for gigs
      min_lines: 100
    - path: api/internal/gig/service.go
      provides: Business logic + GigReader implementation
      min_lines: 80
    - path: api/internal/venue/model.go
      provides: Venue, VenueCreate, VenueUpdate
      min_lines: 40
    - path: api/internal/contact/model.go
      provides: Contact, ContactCreate, ContactUpdate, ContactType enum
      min_lines: 50
  key_links:
    - from: api/internal/gig/model.go
      to: GigReader interface
      via: exported interface consumed by Phase 5, 7, and EPK-10
    - from: api/internal/gig/repository.go
      to: api/internal/platform/db
      via: sqlx DB connection
---

<objective>
Establish the complete data layer for Gig Tracker — SQL migrations, Go models, repository layer, GigReader interface for downstream consumption.

Purpose: Foundation for all Phase 4 plans. Wave 2-4 depend on these packages existing.
Output: 6 migration files, 9 Go packages (model/repository/service/handler for gig/venue/contact)
</objective>

<context>
@.planning/phases/04-gig-tracker/CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Gig status workflow (from CONTEXT.md)
- inquiry → confirmed → advanced → played → cancelled
- cancelled is terminal; all other transitions allowed in both directions
- Payment status: unpaid | deposit_paid | paid | overdue | waived (independent)

# GigReader interface contract
Downstream modules (Finance Tracker, Tour Manager, EPK-10 Import) consume gig data via GigReader.
Interface defined in model.go with concrete implementation in service.go.
</context>

<tasks>

<task type="auto">
  <name>Create SQL migrations for gig/venue/contact tables</name>
  <files>
    api/internal/platform/migrations/005_gigs.sql
    api/internal/platform/migrations/005b_venues.sql
    api/internal/platform/migrations/005c_contacts.sql
    api/internal/platform/migrations/005d_gig_venues.sql
    api/internal/platform/migrations/005e_gig_contacts.sql
    api/internal/platform/migrations/005f_tracklist_gigs.sql
  </files>
  <action>
Create six migration files following the existing goose single-file convention (001_name.sql with Up/Down sections):

005_gigs.sql:
- gig_status enum: inquiry, confirmed, advanced, played, cancelled
- payment_status enum: unpaid, deposit_paid, paid, overdue, waived
- gigs table: id (UUID PK), date, venue, city, country, event_name, promoter_name, promoter_email, promoter_phone, fee_amount, fee_currency (ISO 4217), set_length_minutes, notes, status, payment_status, gig_reader_venue_id (FK nullable), gig_reader_contact_id (FK nullable), created_at, updated_at, deleted_at (soft delete)

005b_venues.sql:
- venues table: id (UUID PK), name, city, country, capacity (nullable int), website (nullable text), tech_contact_name, tech_contact_email, tech_contact_phone, notes, created_at, updated_at, deleted_at

005c_contacts.sql:
- contacts table: id (UUID PK), name, company (nullable), email, phone, type (enum: promoter, agent, label, other), notes, created_at, updated_at, deleted_at

005d_gig_venues.sql:
- gig_venues join table: gig_id (FK gigs.id), venue_id (FK venues.id), is_primary (bool), UNIQUE(gig_id, venue_id)

005e_gig_contacts.sql:
- gig_contacts join table: gig_id (FK gigs.id), contact_id (FK contacts.id), role (text: promoter/agent), UNIQUE(gig_id, contact_id)

005f_tracklist_gigs.sql:
- tracklist_gigs join table: tracklist_id (FK tracklists.id), gig_id (FK gigs.id), UNIQUE(tracklist_id, gig_id)
  </action>
  <verify>
cd api && go run ./cmd/migrate up
psql $DATABASE_URL -c "\\d gigs" | grep -E "id|status|payment"
psql $DATABASE_URL -c "\\d venues" | grep -E "id|name|city"
psql $DATABASE_URL -c "\\d contacts" | grep -E "id|name|email|type"
psql $DATABASE_URL -c "\\d gig_venues"
psql $DATABASE_URL -c "\\d gig_contacts"
psql $DATABASE_URL -c "\\d tracklist_gigs"
  </verify>
  <done>
All 6 migration files created in api/internal/platform/migrations/
Migrations apply successfully with `goose up`
Database schema verified: gigs, venues, contacts tables exist with correct columns
Soft deletion column (deleted_at) present on all tables
</done>
</task>

<task type="auto">
  <name>Create Gig model with GigReader interface</name>
  <files>
    api/internal/gig/model.go
  </files>
  <action>
Create api/internal/gig/model.go with:

type GigStatus string - enum: inquiry, confirmed, advanced, played, cancelled
type PaymentStatus string - enum: unpaid, deposit_paid, paid, overdue, waived

type Gig struct {
  ID UUID
  Date time.Time
  Venue, City, Country string
  EventName string
  PromoterName, PromoterEmail, PromoterPhone string
  FeeAmount decimal.Decimal
  FeeCurrency string (ISO 4217)
  SetLengthMinutes int
  Notes string
  Status GigStatus
  PaymentStatus PaymentStatus
  GigReaderVenueID, GigReaderContactID *UUID (nullable FKs)
  CreatedAt, UpdatedAt time.Time
  DeletedAt *time.Time (soft delete)
}

type GigCreate struct { ... all required fields ... }
type GigUpdate struct { ... all fields optional, includes UpdatedAt for optimistic concurrency ... }

// GigReader interface - consumed by downstream phases
type GigReader interface {
  GetGig(ctx context.Context, id UUID) (*Gig, error)
  ListGigs(ctx context.Context, f GigFilter) ([]*Gig, error)
  Tracklists(ctx context.Context, gigID UUID) ([]*tracklist.Tracklist, error)
  // Future: Venues(), Contacts() as needed
}

// GigFilter for ListGigs: Status, VenueID, City, FeeMin, FeeMax, From, To date filters
  </action>
  <verify>
grep -E "type Gig (struct|{)" api/internal/gig/model.go
grep "type GigReader interface" api/internal/gig/model.go
grep "GigReader" api/internal/gig/model.go | head -5
  </verify>
  <done>
Gig struct has all GIG-01 fields (date, venue, city, country, event name, promoter name/email/phone, fee, currency, notes)
GigReader interface is defined and exported
GigStatus and PaymentStatus enums are defined
Soft deletion via *time.Time DeletedAt field present
</done>
</task>

<task type="auto">
  <name>Create Venue and Contact models</name>
  <files>
    api/internal/venue/model.go
    api/internal/contact/model.go
  </files>
  <action>
Create api/internal/venue/model.go:
type Venue struct {
  ID UUID
  Name, City, Country string
  Capacity *int
  Website *string
  TechContactName, TechContactEmail, TechContactPhone string
  Notes string
  CreatedAt, UpdatedAt time.Time
  DeletedAt *time.Time
}
type VenueCreate, VenueUpdate (with UpdatedAt for optimistic concurrency)

Create api/internal/contact/model.go:
type ContactType string - enum: promoter, agent, label, other
type Contact struct {
  ID UUID
  Name, Company (nullable), Email, Phone string
  Type ContactType
  Notes string
  CreatedAt, UpdatedAt time.Time
  DeletedAt *time.Time
}
type ContactCreate, ContactUpdate (with UpdatedAt)
  </action>
  <verify>
grep "type Venue struct" api/internal/venue/model.go
grep "type Contact struct" api/internal/contact/model.go
grep "type ContactType" api/internal/contact/model.go
  </verify>
  <done>
Venue model with all CONT-01 fields (name, city, country, capacity, website, tech contact info, notes)
Contact model with all CONT-02 fields (name, company, email, phone, type, notes)
Soft deletion on both models
</done>
</task>

<task type="auto">
  <name>Create Gig repository with embedded CRUD pattern</name>
  <files>
    api/internal/gig/repository.go
  </files>
  <action>
Create api/internal/gig/repository.go following the embedded Repo pattern from Phase 1/3:

type gigRepo struct {
  *repo.CRUD[gig, gigCreate, gigUpdate] // embedded generic CRUD
  db *sqlx.DB
}

Constructor: NewGigRepo(db *sqlx.DB) *gigRepo

Methods:
- GetByID(ctx, id) (*Gig, error)
- List(ctx, filter GigFilter) ([]*Gig, error)
- Create(ctx, gig *GigCreate) (*Gig, error)
- Update(ctx, id UUID, u *GigUpdate) (*Gig, error)
- SoftDelete(ctx, id UUID) error
- LinkVenue(ctx, gigID, venueID UUID, isPrimary bool) error
- UnlinkVenue(ctx, gigID, venueID UUID) error
- LinkContact(ctx, gigID, contactID UUID, role string) error
- UnlinkContact(ctx, gigID, contactID UUID) error
- GetLinkedVenueID(ctx, gigID UUID) (*UUID, error)
- GetLinkedContactID(ctx, gigID UUID) (*UUID, error)

GigFilter struct: Status *GigStatus, VenueID, City *string, FeeMin, FeeMax *decimal.Decimal, From, To *time.Time

Use ILIKE for city search, SQL BETWEEN for date range, SQL >/< for fee range.
  </action>
  <verify>
go build ./api/internal/gig/...
grep "func.* gigRepo" api/internal/gig/repository.go | wc -l  # should be 10+
  </verify>
  <done>
Gig repository with all CRUD operations
GigFilter supports GIG-07 filtering (status, venue, city, fee_min, fee_max, from, to)
LinkVenue, LinkContact for GIG-10
Soft delete implemented
</done>
</task>

<task type="auto">
  <name>Create Venue and Contact repositories</name>
  <files>
    api/internal/venue/repository.go
    api/internal/contact/repository.go
  </files>
  <action>
Create api/internal/venue/repository.go:
type venueRepo struct { *repo.CRUD; db *sqlx.DB }
NewVenueRepo(db *sqlx.DB) *venueRepo
Methods: GetByID, List (with optional name/city filter), Create, Update, SoftDelete
Autocomplete: SearchByName(ctx, query string, limit int) ([]*Venue, error) — ILIKE %query% LIMIT

Create api/internal/contact/repository.go:
type contactRepo struct { *repo.CRUD; db *sqlx.DB }
NewContactRepo(db *sqlx.DB) *contactRepo
Methods: GetByID, List (with optional name/type filter), Create, Update, SoftDelete
Autocomplete: SearchByName(ctx, query string, limit int) ([]*Contact, error) — ILIKE %query% LIMIT
  </action>
  <verify>
go build ./api/internal/venue/...
go build ./api/internal/contact/...
grep "Autocomplete\|SearchByName" api/internal/venue/repository.go
grep "Autocomplete\|SearchByName" api/internal/contact/repository.go
  </verify>
  <done>
Venue repository with autocomplete support (CONT-03, CONT-04)
Contact repository with autocomplete support (CONT-03, CONT-04)
Soft deletion on both
</done>
</task>

<task type="auto">
  <name>Create Gig service layer with GigReader implementation</name>
  <files>
    api/internal/gig/service.go
  </files>
  <action>
Create api/internal/gig/service.go:

type GigService struct {
  repo *gigRepo
  venueRepo *venueRepo
  contactRepo *contactRepo
  tracklistRepo tracklistRepoIface (interface for getting tracklists)
}

Constructor: NewGigService(repo *gigRepo, venueRepo *venueRepo, contactRepo *contactRepo, tracklistRepo tracklistRepoIface) *GigService

Methods:
- CreateGig(ctx, *GigCreate) (*Gig, error) — sets default status=inquiry
- GetGig(ctx, id) (*Gig, error) — wraps repo.GetByID
- ListGigs(ctx, GigFilter) ([]*Gig, error)
- UpdateGig(ctx, id UUID, *GigUpdate) (*Gig, error) — validates status transitions (cancelled is terminal)
- DeleteGig(ctx, id) error — soft delete
- LinkVenue(ctx, gigID, venueID UUID, isPrimary bool) error
- LinkContact(ctx, gigID, contactID UUID, role string) error
- GetTracklists(ctx, gigID) ([]*tracklist.Tracklist, error) — from tracklist_gigs join

GigReader interface implementation:
- GetGig(ctx, id) — satisfies GigReader
- ListGigs(ctx, filter) — satisfies GigReader
- Tracklists(ctx, gigID) — satisfies GigReader via tracklist_gigs

Status transition validation:
- If current status == cancelled, reject any status update
- Otherwise allow any status transition (forward or backward)
  </action>
  <verify>
go build ./api/internal/gig/...
grep "GigReader" api/internal/gig/service.go | head -3
grep "cancelled" api/internal/gig/service.go
  </verify>
  <done>
GigService implements all business logic
GigReader interface satisfied (GetGig, ListGigs, Tracklists)
Status transition rules enforced (cancelled is terminal)
Payment status tracked independently (no transition rules)
</done>
</task>

<task type="auto">
  <name>Create Venue and Contact service layers</name>
  <files>
    api/internal/venue/service.go
    api/internal/contact/service.go
  </files>
  <action>
Create api/internal/venue/service.go:
type VenueService struct { repo *venueRepo }
NewVenueService(repo *venueRepo) *VenueService
Methods: CreateVenue, GetVenue, ListVenues, UpdateVenue, DeleteVenue, Autocomplete(ctx, query string, limit int) ([]*Venue, error)

Create api/internal/contact/service.go:
type ContactService struct { repo *contactRepo }
NewContactService(repo *contactRepo) *ContactService
Methods: CreateContact, GetContact, ListContacts, UpdateContact, DeleteContact, Autocomplete(ctx, query string, limit int) ([]*Contact, error)
  </action>
  <verify>
go build ./api/internal/venue/...
go build ./api/internal/contact/...
  </verify>
  <done>
VenueService with all CRUD + Autocomplete
ContactService with all CRUD + Autocomplete
</done>
</task>

<task type="auto">
  <name>Create HTTP handlers and wire routes</name>
  <files>
    api/internal/gig/handler.go
    api/internal/venue/handler.go
    api/internal/contact/handler.go
    api/internal/router.go
  </files>
  <action>
Create api/internal/gig/handler.go:
- GET /api/v1/gigs — ListGigs with query params (status, venue, city, fee_min, fee_max, from, to)
- POST /api/v1/gigs — CreateGig
- GET /api/v1/gigs/:id — GetGig
- PUT /api/v1/gigs/:id — UpdateGig (use UpdatedAt for optimistic concurrency, return 409 on conflict)
- DELETE /api/v1/gigs/:id — DeleteGig (soft delete)
- POST /api/v1/gigs/:id/link-venue — LinkVenue (body: {venue_id, is_primary})
- POST /api/v1/gigs/:id/link-contact — LinkContact (body: {contact_id, role})
- GET /api/v1/gigs/autocomplete — List gigs for "Copy from Previous Gig" (q param, ILIKE)

Create api/internal/venue/handler.go:
- GET /api/v1/venues — ListVenues
- POST /api/v1/venues — CreateVenue
- GET /api/v1/venues/:id — GetVenue
- PUT /api/v1/venues/:id — UpdateVenue
- DELETE /api/v1/venues/:id — DeleteVenue (soft delete)
- GET /api/v1/venues/autocomplete?q= — Autocomplete (min 2 chars, limit 5)

Create api/internal/contact/handler.go:
- GET /api/v1/contacts — ListContacts (filter by type)
- POST /api/v1/contacts — CreateContact
- GET /api/v1/contacts/:id — GetContact
- PUT /api/v1/contacts/:id — UpdateVenue
- DELETE /api/v1/contacts/:id — DeleteContact (soft delete)
- GET /api/v1/contacts/autocomplete?q= — Autocomplete (min 2 chars, limit 5)

Wire in router.go: Register gig, venue, contact route blocks under /api/v1/
  </action>
  <verify>
go build ./api/internal/gig/...
go build ./api/internal/venue/...
go build ./api/internal/contact/...
curl localhost:8080/api/v1/gigs  # should return 200 empty array
curl "localhost:8080/api/v1/venues/autocomplete?q=test"  # should return 200
curl "localhost:8080/api/v1/contacts/autocomplete?q=test"  # should return 200
  </verify>
  <done>
All REST endpoints wired
Gig CRUD with all GIG-01 fields
Venue CRUD + autocomplete (CONT-01, CONT-03, CONT-04)
Contact CRUD + autocomplete (CONT-02, CONT-03, CONT-04)
Optimistic concurrency on PUT (409 on conflict)
Soft delete on DELETE
Gig-venue linking (GIG-10)
Gig-contact linking (GIG-10)
Gig autocomplete for "Copy from Previous Gig" (GIG-08, GIG-11)
</done>
</task>

</tasks>

<verification>
# Smoke tests after migration
curl -X POST localhost:8080/api/v1/gigs \
  -H "Content-Type: application/json" \
  -d '{"date":"2026-05-15","venue":"Berghain","city":"Berlin","country":"DE","event_name":"Saturday Night","fee_amount":2000,"fee_currency":"EUR"}'

curl localhost:8080/api/v1/gigs

curl "localhost:8080/api/v1/venues/autocomplete?q=berg"

curl "localhost:8080/api/v1/contacts/autocomplete?q=promo"
</verification>

<success_criteria>
- [ ] All 6 migration files created and apply cleanly
- [ ] Gig model with all GIG-01 fields + GigReader interface
- [ ] GigService implements GigReader for downstream consumption
- [ ] Venue and Contact models with soft deletion
- [ ] VenueService and ContactService with Autocomplete
- [ ] All REST endpoints return 200 (empty initially, no 500)
- [ ] Gig status transitions enforced (cancelled is terminal)
- [ ] Payment status tracked independently
</success_criteria>

<output>
After completion, create .planning/phases/04-gig-tracker/04-01-SUMMARY.md
</output>