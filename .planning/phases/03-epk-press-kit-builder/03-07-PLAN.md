---
phase: 03-epk-press-kit-builder
plan: "07"
type: execute
wave: 5
depends_on:
  - "03-06"
files_modified: []
autonomous: false
requirements:
  - EPK-01
  - EPK-02
  - EPK-03
  - EPK-04
  - EPK-05
  - EPK-06
  - EPK-07
  - EPK-08
  - EPK-09
  - EPK-10

must_haves:
  truths:
    - "User can navigate to /epk and see all 9 sections rendered on a single scrollable page"
    - "User can write bio text and see the SAVED indicator appear after ~1.5 seconds"
    - "User can upload a press photo and see it appear in the 4-column grid"
    - "User can generate a PDF and see it appear in the export history list"
    - "User can download a previously generated PDF from the export history"
    - "User can toggle section switches and the preference persists after page reload"
  artifacts:
    - path: "apps/dj/app/pages/epk.vue"
      provides: "Full EPK page — all sections functional"
    - path: "api/internal/epk/pdf.go"
      provides: "PDF generator produces readable, formatted output"
  key_links:
    - from: "apps/dj/app/components/epk/EpkExportPanel.vue"
      to: "POST /api/v1/epk/export"
      via: "store.generateExport() → $fetch"
      pattern: "generateExport"
    - from: "POST /api/v1/epk/export"
      to: "api/internal/epk/pdf.go"
      via: "service.GeneratePDF() → renderEPKPDF()"
      pattern: "renderEPKPDF"
---

<objective>
Human verification checkpoint — confirm the full EPK flow works end-to-end in the running application.

Purpose: Visual and functional verification that all 10 EPK requirements are met in the live app. PDF output requires human inspection (visual fidelity cannot be unit tested).
Output: Phase 3 complete verification. Approval or issue list for gap closure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-epk-press-kit-builder/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suites</name>
  <files>api/internal/epk/service_test.go, api/internal/epk/handler_test.go, api/internal/epk/repository_test.go</files>
  <action>
Run both test suites before the human checkpoint to confirm no regressions:

```bash
cd /Users/admin/dev/klubhub-dj/api && go test ./... -count=1
```

```bash
cd /Users/admin/dev/klubhub-dj && pnpm nx test dj
```

Report results. If any failures, fix before presenting the human checkpoint.
  </action>
  <verify>
    <automated>cd /Users/admin/dev/klubhub-dj/api && go test ./... -count=1 && cd /Users/admin/dev/klubhub-dj && pnpm nx test dj</automated>
  </verify>
  <done>Go tests green; Vitest green; no regressions from EPK additions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of EPK UI and flows</name>
  <action>Present the verification steps below to the user and wait for their approval signal.</action>
  <what-built>
Phase 3 EPK / Press Kit Builder — complete implementation:
- Go backend: migration 004 (epk_content + epk_exports), EPK package (model, repository, service, handler, pdf.go), routes at /api/v1/epk/*
- Pinia store (useEpkStore) + auto-save composable (useEpkAutosave)
- 10 EPK section components + epk.vue page + EpkPageHeader
- TheNav + TheBottomNav updated with EPK entry
- PDF generation with codeberg.org/go-pdf/fpdf: white document, DJ logo in header, accent colour from settings, Markdown rendering for long bio, 3-column press photo grid
- Export history (MinIO storage, presigned download URLs)
  </what-built>
  <how-to-verify>
Start the application: `docker compose up -d` (or dev server if already running).

**EPK-01 — Bio editor:**
1. Navigate to http://localhost:3000/epk
2. Confirm 9 sections visible stacked vertically in order: ARTIST BIO → PRESS PHOTOS → TECH RIDER → STAGE PLOT → GIG HIGHLIGHTS → PRESS QUOTES → SOCIAL LINKS → CONTACT INFO → EXPORT PANEL
3. Type in the SHORT BIO field — wait 1.5s — confirm "SAVED" appears in the page header
4. Type Markdown in the LONG BIO textarea (e.g., `## My Headline`) — confirm it saves

**EPK-02 — Press photos:**
5. Drag an image file onto the PRESS PHOTOS drop zone — confirm it appears in the 4-column grid
6. Hover over a photo — confirm × delete button appears; click it — confirm photo is removed

**EPK-03 — Tech rider + stage plot:**
7. Enter text in the TECH RIDER textarea — confirm it saves
8. Upload an image to the STAGE PLOT zone — confirm image preview appears

**EPK-04/05 — Other content:**
9. Add a gig highlight entry — confirm it appears in the list; add a press quote (text + source) — confirm it appears
10. Confirm "IMPORT FROM GIGS" button is visible but disabled with tooltip "Coming Soon — available after Gig Tracker is set up"

**EPK-06/07 — PDF generation:**
11. In the EXPORT PANEL, toggle off one section (e.g., STAGE PLOT) — confirm switch changes
12. Click EXPORT PDF — confirm loading state (progress bar / disabled button)
13. PDF appears in the export history list below
14. Open the generated PDF — confirm: white background, DJ name + logo in header (if set in settings), section headings in accent colour, correct sections included/excluded per toggles
15. Confirm long bio Markdown is rendered (## heading appears as larger text, **bold** appears bold)
16. Confirm press photos appear in 3-column grid in PDF

**EPK-07 — Section toggle persistence:**
17. Reload the page — confirm section toggles retain their state

**EPK-08 — Export history:**
18. Click DOWNLOAD on a history item — confirm PDF opens/downloads
19. Click DELETE on a history item — confirm it is removed from the list

**EPK-09 — Data persistence:**
20. Reload the page — confirm all previously entered bio, highlights, quotes, and social links are still populated

**EPK-10 — Import from Gigs stub:**
21. Confirm GIG HIGHLIGHTS section has "IMPORT FROM GIGS" button that is disabled and shows the "Coming Soon" tooltip
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 10 EPK requirements verified by human:
- EPK-01: Bio editor (short plain text + long Markdown) with auto-save
- EPK-02: Press photo grid (20 max, drag-and-drop, delete)
- EPK-03: Tech rider textarea + stage plot image upload
- EPK-04: Social/streaming links with auto-save
- EPK-05: Gig highlights list + press quotes list + contact info
- EPK-06: PDF generated with logo, accent colour, Markdown rendered
- EPK-07: Section include/exclude toggles persist
- EPK-08: Export history with download and delete
- EPK-09: Data persists across page reload
- EPK-10: Import from Gigs stub visible but disabled
</verification>

<success_criteria>
Human approves all 10 EPK requirements verified in the running application.
Go test suite and Vitest suite both green with no regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-epk-press-kit-builder/03-07-SUMMARY.md`
</output>
